"""Split settings (design §1.1).

Choose the module with DJANGO_SETTINGS_MODULE:

    config.settings.dev    local development (the default in manage.py)
    config.settings.test   pytest
    config.settings.prod   production (Phase 9)
"""
