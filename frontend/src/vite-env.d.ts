/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

// The PWA plugin's virtual module. Declared here so `register.ts` typechecks
// whether or not the plugin is enabled for a given build (T8.1).
